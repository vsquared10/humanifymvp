<nav id="mobile-menu" class="navbar-slideout">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
      <ul class="nav navbar-nav avatar text-center">
        <% if user_signed_in? %>
          <!-- Username & Kudos Balance -->
          <%= link_to profile_path  do %>
            <li class="panel">
              <div class="panel-body">
                <%= image_tag(current_user.user_img,
                              width: 70,
                              class: "img-circle",
                              id: "user-avatar") %>

                <h3><%= current_user.name %></h3>
                <h2> KP: <%= current_user.points %> </h2>
              </div>
            </li>
          <% end %>
          <br>
          <li><%= link_to 'New Listing', new_listing_path %></li>
          <br>
          <li> 
            <div class="btn-group" role="group" aria-label="...">
              <%= link_to edit_user_registration_path do %>
                <button type="button" class="btn btn-default">Settings</button>
              <% end %>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <button type="button" class="btn btn-default">Log out</button>
              <% end %>
            </div>
          </li>
        <% else %>
          <!-- Sign-Up & Sign-In -->
          <li data-toggle="" data-target="">
            <%= image_tag('elements-11.png', id: "user-avatar") %>
          </li>
        <% end %>
      </ul>

    <!-- Brand and toggle get grouped for better mobile display -->
      <% link_to image_tag('humanify-a.png'), root_path, id:"small-logo", class: "navbar-brand"%><!-- image_tag gives huge image;doesn't resize appropriately -->
  </div><!-- /.container-fluid -->
</nav>
